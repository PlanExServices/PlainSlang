# Server edition (optional, dormant)

Dockerfile for the Node+Postgres server edition — moved out of the repo root.
The PRIMARY deployment is the static edition on Cloudflare Pages (`site/`,
no build command). See the main README.
