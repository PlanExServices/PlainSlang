# Render.com blueprint — PlainSlang server edition (free tier).
services:
  - type: web
    name: plainslang
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.20.2
