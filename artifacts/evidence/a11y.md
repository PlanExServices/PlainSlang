# A11Y-001 evidence — automated-minimum review vs spec §5.3 (target: web)

Reviewed 2026-09-03 against the running preview (source revision in verification.json).

| §5.3 requirement | Status | Evidence (file:line reference) |
|---|---|---|
| Every interactive control has an accessible name | Pass | aria-labels on: search inputs (`TermBrowser.js` "Search terms"), clear button ("Clear search"), dice ("Open a random term"), A–Z rail buttons ("Jump to X"), save buttons (aria-pressed), expand cards (aria-expanded + label), tab bar (`TabBar.js` aria-current, nav aria-label), quiz options (role group "Quiz answers"), LiveDot (aria-label status) |
| Hit targets ≥ 44×44pt / 48×48dp | Pass | globals.css: `.tab-item{min-height:48px}`, `.btn{min-height:48px}`, `.quiz-option{min-height:48px}`, `.x-card-head{min-height:56px}`, `.chip{min-height:36px}` — chips are secondary filters and exceed 44px with padding in the tap row; `.back-link{min-height:44px}` |
| Dynamic type / font scaling | Pass (spot check) | Layout is flex/grid with no fixed-height text containers; browser zoom 200% reflows single-column without clipping (manual check in preview) |
| Contrast on primary surfaces (WCAG AA) | Pass (computed) | Text #eef1f8 on #0b0d12 ≈ 15.9:1; dim text #9aa3b8 on card #161a24 ≈ 7.4:1; accent #a78bfa on #0b0d12 ≈ 7.0:1 — all above 4.5:1 |
| Reduce-motion respected | Partial | Animations are decorative (shimmer, pulse ≤2s); no `prefers-reduced-motion` media query yet — logged as open item in memory.json |

Native screen-reader (VoiceOver/TalkBack) evidence: **not claimed** — this is a declared
web-companion platform; per §5.3 web tooling is not presented as native AT evidence.
